import type React from "react";
import { getSliceData } from "../lib/sliceGeometry";

const WHEEL_CENTER = 250;
const WHEEL_RADIUS = 200;

function Wheel({ items }: { items: string[] }): React.JSX.Element {
  return <></>;
}

export default Wheel;
